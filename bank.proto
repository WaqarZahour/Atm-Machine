package tutorial;

message Person {
  required string name = 1;
  required string account_no = 2;
  required string pin_no = 3;
  required int32 amount = 4;
}

message BankAccounts {
  repeated Person person = 1;
}
